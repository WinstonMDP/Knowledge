#import "cfg.typ": cfg
#show: cfg

= Алгебра
$(V, +, *)$ - алгебра над полем $F$:
- $(V, +, *)$ - векторное пространство над полем $F$
- $(V, +, *)$ - кольцо
- $(lambda a) b = a(lambda b) = lambda(a b)$

Всякое поле $L$, содержащее $K$ в качестве подполя, можно рассматривать как алгебру над
$K$. В частности, поле $CC$ есть алгебра над $RR$.

$a b = sum_(i, j = 1)^n a_i b_j (e_i e_j)$

Если произведение базисных векторов коммутативно, то и всех векторов тоже. То же про
ассоциативность.
